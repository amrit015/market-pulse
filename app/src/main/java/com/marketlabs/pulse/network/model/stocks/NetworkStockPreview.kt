package com.marketlabs.pulse.network.model.stocks

// Generated by Claude Code.

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 💡 THOUGHT PROCESS:
 * Response body for `GET /stocks/{symbol}` — a single item of the same shape returned inside
 * `GET /stocks/previews`' `previews` array. Replaces `NetworkStockDeepStudy`: the backend split
 * one merged deep-study document into a small, always-cached PREVIEW (this file, ~2 KB, drives
 * the list/collapsed card) and a large DETAIL fetched only on tap (`NetworkStockDetail.kt`,
 * ~12 KB). See `stockAnalysisEngine.ts`'s "FIRESTORE DOCUMENTS WRITTEN" header comment for the
 * authoritative preview/detail field split.
 *
 * Unlike the old `/stocks/{symbol}` response, `symbol` IS part of this body now — the batch
 * `/stocks/previews` endpoint filters on `d?.symbol` being present, so every list item is
 * guaranteed to carry it. Still modeled nullable per this codebase's DTO convention; the mapper
 * defensively skips any entry missing it rather than trusting a non-null assertion for a Room
 * primary key (see `StockMappers.kt`).
 */
@JsonClass(generateAdapter = true)
data class NetworkStockPreview(
    val symbol: String? = null,
    val name: String? = null,

    @Json(name = "schema_version")
    val schemaVersion: Int? = null,

    @Json(name = "analysis_date")
    val analysisDate: String? = null,

    val price: Double? = null,

    @Json(name = "change_percent")
    val changePercent: Double? = null,

    @Json(name = "plain_read")
    val plainRead: String? = null,

    /** One of BREAKDOWN | BREAKOUT | ACCUMULATION | DISTRIBUTION | MEAN_REVERSION | RANGE_BOUND. */
    @Json(name = "technical_setup")
    val technicalSetup: String? = null,

    @Json(name = "setup_net_bias")
    val setupNetBias: Int? = null,

    @Json(name = "setup_confidence")
    val setupConfidence: Int? = null,

    /** The chip vocabulary WITHOUT plain-English `meaning` — that only exists on the detail doc's `condition_labels`. */
    @Json(name = "condition_chips")
    val conditionChips: List<NetworkConditionChip>? = null,

    /** One of risk_on | risk_off | neutral. */
    @Json(name = "regime_at_analysis")
    val regimeAtAnalysis: String? = null,

    // --- Day-over-day delta (computeTimelineDelta in timeline.ts) — null/false/[] on a symbol's first-ever run ---
    @Json(name = "previous_close")
    val previousClose: Double? = null,

    @Json(name = "previous_setup")
    val previousSetup: String? = null,

    @Json(name = "previous_net_bias")
    val previousNetBias: Int? = null,

    @Json(name = "setup_changed")
    val setupChanged: Boolean? = null,

    @Json(name = "chips_added")
    val chipsAdded: List<String>? = null,

    @Json(name = "chips_removed")
    val chipsRemoved: List<String>? = null,

    @Json(name = "headline_metrics")
    val headlineMetrics: NetworkHeadlineMetrics? = null,

    @Json(name = "has_direct_news")
    val hasDirectNews: Boolean? = null,

    @Json(name = "top_headline")
    val topHeadline: String? = null,

    /** Compliance-sanitizer flags (sanitiser.ts); null when clean rather than an empty list. */
    @Json(name = "content_flags")
    val contentFlags: List<String>? = null,

    /** Epoch millis. Compared against the cached detail's own `detail_version` to know if a re-fetch is needed. */
    @Json(name = "detail_version")
    val detailVersion: Long? = null,

    /** Epoch millis — this is the field the app actually uses for "as of" display, not `last_updated`. */
    val timestamp: Long? = null

    // 💡 `last_updated` (a pre-formatted string, e.g. "August 7, 2026 at 6:15:44 PM UTC-7") exists
    // in the raw response but is deliberately NOT modeled here — per the backend, it's there for
    // human readability when eyeballing Firestore directly, not for clients to consume. `timestamp`
    // above is the field the app actually fetches and formats client-side.
)

@JsonClass(generateAdapter = true)
data class NetworkConditionChip(
    val label: String? = null,

    /** One of BULLISH | BEARISH | NEUTRAL. */
    val direction: String? = null,

    /** One of MOMENTUM | TREND | VOLUME | VOLATILITY | VALUATION | EVENT. */
    val category: String? = null,

    /** Non-null only when regime-qualification downgraded this label's direction to NEUTRAL. */
    @Json(name = "qualified_by")
    val qualifiedBy: String? = null
)

@JsonClass(generateAdapter = true)
data class NetworkHeadlineMetrics(
    @Json(name = "dist_sma200_pct")
    val distSma200Pct: Double? = null,

    val rvol: Double? = null,

    @Json(name = "atr_pct")
    val atrPct: Double? = null,

    @Json(name = "range_position_52w")
    val rangePosition52w: Double? = null,

    val rsi: Double? = null
)

/**
 * Response body for `GET /stocks/previews` — every analyzed symbol's preview in one call.
 * Replaces the old `/stocks/tracked` + N×`/stocks/{symbol}` fetch pattern entirely (see the
 * backend's own comment on this route: "Replaces the N+1 pattern").
 */
@JsonClass(generateAdapter = true)
data class NetworkStockPreviewsResponse(
    val previews: List<NetworkStockPreview>? = null,
    val count: Int? = null
)
