package com.marketlabs.pulse.network.store.stocks

// Generated by Claude Code.

import com.marketlabs.pulse.storage.model.stocks.StockAnalysis

interface RemoteStockDataSource {

    /**
     * Fetches the list of actively tracked ticker symbols from `stock_config/master`.
     * Falls back to the backend's default Magnificent 7 basket if the config doc is missing.
     */
    suspend fun getTrackedSymbols(): Result<List<String>>

    /**
     * Fetches the latest `market_stocks/{TICKER}` document for each given symbol, in parallel.
     * Symbols with no document yet (e.g. never analyzed) are silently omitted from the result list.
     */
    suspend fun getStockAnalysis(symbols: List<String>): Result<List<StockAnalysis>>
}
