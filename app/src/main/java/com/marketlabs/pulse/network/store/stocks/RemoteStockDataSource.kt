package com.marketlabs.pulse.network.store.stocks

// Generated by Claude Code.

import com.marketlabs.pulse.storage.model.stocks.StockDetail
import com.marketlabs.pulse.storage.model.stocks.StockPreview

interface RemoteStockDataSource {

    /** Fetches every analyzed symbol's preview from `GET /stocks/previews` in one call. */
    suspend fun getStockPreviews(): Result<List<StockPreview>>

    /** Fetches one symbol's full detail from `GET /stocks/{symbol}/detail`. */
    suspend fun getStockDetail(symbol: String): Result<StockDetail>
}
