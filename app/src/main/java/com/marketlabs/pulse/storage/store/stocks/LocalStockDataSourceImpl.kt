package com.marketlabs.pulse.storage.store.stocks

// Generated by Claude Code.

import com.marketlabs.pulse.storage.database.dao.StocksDao
import com.marketlabs.pulse.storage.model.stocks.StockDetail
import com.marketlabs.pulse.storage.model.stocks.StockPreview
import com.marketlabs.pulse.storage.model.stocks.mappers.toDomain
import com.marketlabs.pulse.storage.model.stocks.mappers.toEntity
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.map
import javax.inject.Inject

class LocalStockDataSourceImpl @Inject constructor(
    private val dao: StocksDao
) : LocalStockDataSource {

    override fun getStockPreviewsStream(): Flow<List<StockPreview>> {
        return dao.getStockPreviewsStream().map { entities -> entities.map { it.toDomain() } }
    }

    override suspend fun savePreviews(previews: List<StockPreview>) {
        dao.insertPreviews(previews.map { it.toEntity() })
    }

    override suspend fun getLastSyncedTimestamp(): Long? {
        return dao.getLastSyncedTimestamp()
    }

    override suspend fun updateLastSyncedTimestamp(timestamp: Long) {
        dao.updateLastSyncedTimestamp(timestamp)
    }

    override suspend fun getPreviewDetailVersion(symbol: String): Long? {
        return dao.getPreviewDetailVersion(symbol)
    }

    override fun getStockDetailStream(symbol: String): Flow<StockDetail?> {
        return dao.getStockDetailStream(symbol).map { entity -> entity?.toDomain() }
    }

    override suspend fun saveDetail(detail: StockDetail) {
        dao.insertDetail(detail.toEntity())
    }

    override suspend fun getCachedDetailVersion(symbol: String): Long? {
        return dao.getCachedDetailVersion(symbol)
    }
}
