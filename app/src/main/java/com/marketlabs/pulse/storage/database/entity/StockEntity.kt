package com.marketlabs.pulse.storage.database.entity

// Generated by Claude Code.

import androidx.room.Entity
import androidx.room.PrimaryKey
import androidx.room.TypeConverters
import com.marketlabs.pulse.storage.database.converters.StocksConverters
import com.marketlabs.pulse.storage.model.stocks.DomainBattlegroundLevels
import com.marketlabs.pulse.storage.model.stocks.DomainContextVault
import com.marketlabs.pulse.storage.model.stocks.DomainExecutiveThesis
import com.marketlabs.pulse.storage.model.stocks.DomainStockNewsItem
import com.marketlabs.pulse.storage.model.stocks.DomainTechnicalIndicators

/**
 * 💡 THOUGHT PROCESS:
 * Unlike `IndicatorsEntity` (one row per day, `dateId` as primary key), this table has one row
 * per tracked symbol — structurally it's closer to `AssetOverviewEntity` (dashboard's
 * `dashboard_assets` table). We follow `AssetOverviewEntity`'s multi-row-keyed-by-symbol shape,
 * but follow `IndicatorsEntity`'s JSON-blob-per-nested-object storage for the AI-enriched fields
 * (`technicalIndicators`, `executiveThesis`, `topNewsStream`, `battlegroundLevels`, `contextVault`)
 * rather than flattening them into dozens of individual columns.
 */
@Entity(tableName = "market_stocks")
@TypeConverters(StocksConverters::class)
data class StockEntity(
    @PrimaryKey val symbol: String,
    val lastSyncedTimestamp: Long,

    val price: Double? = null,
    val changePercent: Double? = null,
    val technicalIndicators: DomainTechnicalIndicators? = null,
    val technicalSetup: String? = null,
    val executiveThesis: DomainExecutiveThesis? = null,
    val topNewsStream: List<DomainStockNewsItem>? = null,
    val battlegroundLevels: DomainBattlegroundLevels? = null,
    val contextVault: DomainContextVault? = null,
    val timestamp: Long? = null
)
