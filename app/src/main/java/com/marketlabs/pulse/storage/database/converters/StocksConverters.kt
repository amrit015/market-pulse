package com.marketlabs.pulse.storage.database.converters

// Generated by Claude Code.

import androidx.room.TypeConverter
import com.marketlabs.pulse.storage.model.stocks.DomainCallRecord
import com.marketlabs.pulse.storage.model.stocks.DomainConditionChip
import com.marketlabs.pulse.storage.model.stocks.DomainConditionLabel
import com.marketlabs.pulse.storage.model.stocks.DomainContextVault
import com.marketlabs.pulse.storage.model.stocks.DomainDeepDiveSection
import com.marketlabs.pulse.storage.model.stocks.DomainDigestSection
import com.marketlabs.pulse.storage.model.stocks.DomainExecutiveThesis
import com.marketlabs.pulse.storage.model.stocks.DomainFundamentalsDelta
import com.marketlabs.pulse.storage.model.stocks.DomainFundamentals
import com.marketlabs.pulse.storage.model.stocks.DomainHeadlineMetrics
import com.marketlabs.pulse.storage.model.stocks.DomainLevels
import com.marketlabs.pulse.storage.model.stocks.DomainMacro
import com.marketlabs.pulse.storage.model.stocks.DomainScenario
import com.marketlabs.pulse.storage.model.stocks.DomainSetupSignal
import com.marketlabs.pulse.storage.model.stocks.DomainStockNewsItem
import com.marketlabs.pulse.storage.model.stocks.DomainTechnicalIndicators
import com.marketlabs.pulse.storage.model.stocks.DomainWatchItem
import com.squareup.moshi.Moshi
import com.squareup.moshi.Types
import com.squareup.moshi.kotlin.reflect.KotlinJsonAdapterFactory

/**
 * Room can't natively persist nested data classes, so each nested object/list on
 * `StockPreviewEntity`/`StockDetailEntity` is (de)serialized to/from a JSON string column — same
 * approach as `IndicatorsConverters` for the indicators domain. One converter pair per distinct
 * Kotlin type.
 *
 * `List<String>?` columns (`chipsAdded`, `chipsRemoved`, `contentFlags`, `setupConfirming`,
 * `setupConflicting`, `considerations`) deliberately have NO converter defined here —
 * `NewsConverters.fromStringList`/`toStringList` already registers that exact conversion on
 * `AppDatabase`, and Room resolves `@TypeConverters` database-wide, not scoped to the entity/class
 * that declares them. A second `List<String>?` pair here would be a duplicate conversion and fails
 * KSP's `kspDebugKotlin` step with "Multiple functions define the same conversion."
 */
class StocksConverters {
    private val moshi = Moshi.Builder().add(KotlinJsonAdapterFactory()).build()

    // --- Condition Chips (preview) ---
    private val conditionChipListType = Types.newParameterizedType(List::class.java, DomainConditionChip::class.java)
    private val conditionChipListAdapter = moshi.adapter<List<DomainConditionChip>>(conditionChipListType)

    @TypeConverter
    fun fromConditionChips(data: List<DomainConditionChip>?): String? =
        data?.let { conditionChipListAdapter.toJson(it) }

    @TypeConverter
    fun toConditionChips(json: String?): List<DomainConditionChip>? =
        json?.let { conditionChipListAdapter.fromJson(it) }

    // --- Headline Metrics (preview) ---
    @TypeConverter
    fun fromHeadlineMetrics(data: DomainHeadlineMetrics?): String? =
        data?.let { moshi.adapter(DomainHeadlineMetrics::class.java).toJson(it) }

    @TypeConverter
    fun toHeadlineMetrics(json: String?): DomainHeadlineMetrics? =
        json?.let { moshi.adapter(DomainHeadlineMetrics::class.java).fromJson(it) }

    // --- Technical Indicators (detail) ---
    @TypeConverter
    fun fromTechnicalIndicators(data: DomainTechnicalIndicators?): String? =
        data?.let { moshi.adapter(DomainTechnicalIndicators::class.java).toJson(it) }

    @TypeConverter
    fun toTechnicalIndicators(json: String?): DomainTechnicalIndicators? =
        json?.let { moshi.adapter(DomainTechnicalIndicators::class.java).fromJson(it) }

    // --- Levels (detail) ---
    @TypeConverter
    fun fromLevels(data: DomainLevels?): String? =
        data?.let { moshi.adapter(DomainLevels::class.java).toJson(it) }

    @TypeConverter
    fun toLevels(json: String?): DomainLevels? =
        json?.let { moshi.adapter(DomainLevels::class.java).fromJson(it) }

    // --- Setup Signals (detail) ---
    private val setupSignalListType = Types.newParameterizedType(List::class.java, DomainSetupSignal::class.java)
    private val setupSignalListAdapter = moshi.adapter<List<DomainSetupSignal>>(setupSignalListType)

    @TypeConverter
    fun fromSetupSignals(data: List<DomainSetupSignal>?): String? =
        data?.let { setupSignalListAdapter.toJson(it) }

    @TypeConverter
    fun toSetupSignals(json: String?): List<DomainSetupSignal>? =
        json?.let { setupSignalListAdapter.fromJson(it) }

    // --- Condition Labels (detail) ---
    private val conditionLabelListType = Types.newParameterizedType(List::class.java, DomainConditionLabel::class.java)
    private val conditionLabelListAdapter = moshi.adapter<List<DomainConditionLabel>>(conditionLabelListType)

    @TypeConverter
    fun fromConditionLabels(data: List<DomainConditionLabel>?): String? =
        data?.let { conditionLabelListAdapter.toJson(it) }

    @TypeConverter
    fun toConditionLabels(json: String?): List<DomainConditionLabel>? =
        json?.let { conditionLabelListAdapter.fromJson(it) }

    // --- Watch List (detail) ---
    private val watchItemListType = Types.newParameterizedType(List::class.java, DomainWatchItem::class.java)
    private val watchItemListAdapter = moshi.adapter<List<DomainWatchItem>>(watchItemListType)

    @TypeConverter
    fun fromWatchList(data: List<DomainWatchItem>?): String? =
        data?.let { watchItemListAdapter.toJson(it) }

    @TypeConverter
    fun toWatchList(json: String?): List<DomainWatchItem>? =
        json?.let { watchItemListAdapter.fromJson(it) }

    // --- Fundamentals (detail) ---
    @TypeConverter
    fun fromFundamentals(data: DomainFundamentals?): String? =
        data?.let { moshi.adapter(DomainFundamentals::class.java).toJson(it) }

    @TypeConverter
    fun toFundamentals(json: String?): DomainFundamentals? =
        json?.let { moshi.adapter(DomainFundamentals::class.java).fromJson(it) }

    // --- Macro (detail) ---
    @TypeConverter
    fun fromMacro(data: DomainMacro?): String? =
        data?.let { moshi.adapter(DomainMacro::class.java).toJson(it) }

    @TypeConverter
    fun toMacro(json: String?): DomainMacro? =
        json?.let { moshi.adapter(DomainMacro::class.java).fromJson(it) }

    // --- Scenarios (detail) ---
    private val scenarioListType = Types.newParameterizedType(List::class.java, DomainScenario::class.java)
    private val scenarioListAdapter = moshi.adapter<List<DomainScenario>>(scenarioListType)

    @TypeConverter
    fun fromScenarios(data: List<DomainScenario>?): String? =
        data?.let { scenarioListAdapter.toJson(it) }

    @TypeConverter
    fun toScenarios(json: String?): List<DomainScenario>? =
        json?.let { scenarioListAdapter.fromJson(it) }

    // --- Executive Thesis (detail) ---
    @TypeConverter
    fun fromExecutiveThesis(data: DomainExecutiveThesis?): String? =
        data?.let { moshi.adapter(DomainExecutiveThesis::class.java).toJson(it) }

    @TypeConverter
    fun toExecutiveThesis(json: String?): DomainExecutiveThesis? =
        json?.let { moshi.adapter(DomainExecutiveThesis::class.java).fromJson(it) }

    // --- Top News Stream (detail) ---
    private val newsListType = Types.newParameterizedType(List::class.java, DomainStockNewsItem::class.java)
    private val newsListAdapter = moshi.adapter<List<DomainStockNewsItem>>(newsListType)

    @TypeConverter
    fun fromTopNewsStream(data: List<DomainStockNewsItem>?): String? =
        data?.let { newsListAdapter.toJson(it) }

    @TypeConverter
    fun toTopNewsStream(json: String?): List<DomainStockNewsItem>? =
        json?.let { newsListAdapter.fromJson(it) }

    // --- Context Vault (detail) ---
    @TypeConverter
    fun fromContextVault(data: DomainContextVault?): String? =
        data?.let { moshi.adapter(DomainContextVault::class.java).toJson(it) }

    @TypeConverter
    fun toContextVault(json: String?): DomainContextVault? =
        json?.let { moshi.adapter(DomainContextVault::class.java).fromJson(it) }

    // --- Forward Calls (detail) ---
    @TypeConverter
    fun fromCallRecord(data: DomainCallRecord?): String? =
        data?.let { moshi.adapter(DomainCallRecord::class.java).toJson(it) }

    @TypeConverter
    fun toCallRecord(json: String?): DomainCallRecord? =
        json?.let { moshi.adapter(DomainCallRecord::class.java).fromJson(it) }

    // --- Daily Digest Sections (detail) ---
    private val digestSectionListType = Types.newParameterizedType(List::class.java, DomainDigestSection::class.java)
    private val digestSectionListAdapter = moshi.adapter<List<DomainDigestSection>>(digestSectionListType)

    @TypeConverter
    fun fromDigestSections(data: List<DomainDigestSection>?): String? =
        data?.let { digestSectionListAdapter.toJson(it) }

    @TypeConverter
    fun toDigestSections(json: String?): List<DomainDigestSection>? =
        json?.let { digestSectionListAdapter.fromJson(it) }

    // --- Deep Dive Sections (deep dive) ---
    private val deepDiveSectionListType = Types.newParameterizedType(List::class.java, DomainDeepDiveSection::class.java)
    private val deepDiveSectionListAdapter = moshi.adapter<List<DomainDeepDiveSection>>(deepDiveSectionListType)

    @TypeConverter
    fun fromDeepDiveSections(data: List<DomainDeepDiveSection>?): String? =
        data?.let { deepDiveSectionListAdapter.toJson(it) }

    @TypeConverter
    fun toDeepDiveSections(json: String?): List<DomainDeepDiveSection>? =
        json?.let { deepDiveSectionListAdapter.fromJson(it) }

    // --- Fundamentals Delta (deep dive) ---
    private val fundamentalsDeltaListType = Types.newParameterizedType(List::class.java, DomainFundamentalsDelta::class.java)
    private val fundamentalsDeltaListAdapter = moshi.adapter<List<DomainFundamentalsDelta>>(fundamentalsDeltaListType)

    @TypeConverter
    fun fromFundamentalsDelta(data: List<DomainFundamentalsDelta>?): String? =
        data?.let { fundamentalsDeltaListAdapter.toJson(it) }

    @TypeConverter
    fun toFundamentalsDelta(json: String?): List<DomainFundamentalsDelta>? =
        json?.let { fundamentalsDeltaListAdapter.fromJson(it) }
}
