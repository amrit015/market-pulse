package com.marketlabs.pulse.storage.store.stocks

// Generated by Claude Code.

import com.marketlabs.pulse.storage.model.stocks.StockDetail
import com.marketlabs.pulse.storage.model.stocks.StockPreview
import kotlinx.coroutines.flow.Flow

interface LocalStockDataSource {

    fun getStockPreviewsStream(): Flow<List<StockPreview>>
    suspend fun savePreviews(previews: List<StockPreview>)

    suspend fun getLastSyncedTimestamp(): Long?
    suspend fun updateLastSyncedTimestamp(timestamp: Long)

    /** Null if this symbol has no cached preview yet. */
    suspend fun getPreviewDetailVersion(symbol: String): Long?

    /** Null until this symbol's detail has been fetched at least once. */
    fun getStockDetailStream(symbol: String): Flow<StockDetail?>
    suspend fun saveDetail(detail: StockDetail)

    /** Null if this symbol's detail has never been fetched. */
    suspend fun getCachedDetailVersion(symbol: String): Long?
}
