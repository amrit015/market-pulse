package com.marketlabs.pulse.storage.model.stocks.mappers

// Generated by Claude Code.

import com.marketlabs.pulse.network.model.stocks.NetworkCallBasisStat
import com.marketlabs.pulse.network.model.stocks.NetworkCallRecord
import com.marketlabs.pulse.network.model.stocks.NetworkCallStats
import com.marketlabs.pulse.network.model.stocks.NetworkConditionChip
import com.marketlabs.pulse.network.model.stocks.NetworkConditionLabel
import com.marketlabs.pulse.network.model.stocks.NetworkContextVault
import com.marketlabs.pulse.network.model.stocks.NetworkEventLogItem
import com.marketlabs.pulse.network.model.stocks.NetworkExecutiveThesis
import com.marketlabs.pulse.network.model.stocks.NetworkForwardCall
import com.marketlabs.pulse.network.model.stocks.NetworkFundamentals
import com.marketlabs.pulse.network.model.stocks.NetworkHeadlineMetrics
import com.marketlabs.pulse.network.model.stocks.NetworkLevels
import com.marketlabs.pulse.network.model.stocks.NetworkMacro
import com.marketlabs.pulse.network.model.stocks.NetworkReturns
import com.marketlabs.pulse.network.model.stocks.NetworkScenario
import com.marketlabs.pulse.network.model.stocks.NetworkSetupSignal
import com.marketlabs.pulse.network.model.stocks.NetworkStockDetail
import com.marketlabs.pulse.network.model.stocks.NetworkStockNewsItem
import com.marketlabs.pulse.network.model.stocks.NetworkStockPreview
import com.marketlabs.pulse.network.model.stocks.NetworkTechnicalIndicators
import com.marketlabs.pulse.network.model.stocks.NetworkWatchItem
import com.marketlabs.pulse.storage.database.entity.StockDetailEntity
import com.marketlabs.pulse.storage.database.entity.StockPreviewEntity
import com.marketlabs.pulse.storage.model.stocks.DomainCallBasisStat
import com.marketlabs.pulse.storage.model.stocks.DomainCallRecord
import com.marketlabs.pulse.storage.model.stocks.DomainCallStats
import com.marketlabs.pulse.storage.model.stocks.DomainConditionChip
import com.marketlabs.pulse.storage.model.stocks.DomainConditionLabel
import com.marketlabs.pulse.storage.model.stocks.DomainContextVault
import com.marketlabs.pulse.storage.model.stocks.DomainEventLogItem
import com.marketlabs.pulse.storage.model.stocks.DomainExecutiveThesis
import com.marketlabs.pulse.storage.model.stocks.DomainForwardCall
import com.marketlabs.pulse.storage.model.stocks.DomainFundamentals
import com.marketlabs.pulse.storage.model.stocks.DomainHeadlineMetrics
import com.marketlabs.pulse.storage.model.stocks.DomainLevels
import com.marketlabs.pulse.storage.model.stocks.DomainMacro
import com.marketlabs.pulse.storage.model.stocks.DomainReturns
import com.marketlabs.pulse.storage.model.stocks.DomainScenario
import com.marketlabs.pulse.storage.model.stocks.DomainSetupSignal
import com.marketlabs.pulse.storage.model.stocks.DomainStockNewsItem
import com.marketlabs.pulse.storage.model.stocks.DomainTechnicalIndicators
import com.marketlabs.pulse.storage.model.stocks.DomainWatchItem
import com.marketlabs.pulse.storage.model.stocks.StockDetail
import com.marketlabs.pulse.storage.model.stocks.StockPreview

// ==========================================
// 🌐 NETWORK TO DOMAIN — PREVIEW
// ==========================================

/**
 * Returns null when `symbol` is missing from the payload. `/stocks/previews` already filters to
 * entries with a symbol server-side, so this is a defensive backstop, not the expected path — it
 * exists so a Room primary key never has to fall back to a non-null assertion. Callers should
 * `mapNotNull` a list of these (see `RemoteStockDataSourceImpl`).
 */
fun NetworkStockPreview.toDomain(lastSyncedTimestamp: Long): StockPreview? {
    val resolvedSymbol = symbol ?: return null
    return StockPreview(
        symbol = resolvedSymbol,
        lastSyncedTimestamp = lastSyncedTimestamp,
        name = name,
        schemaVersion = schemaVersion,
        analysisDate = analysisDate,
        price = price,
        changePercent = changePercent,
        plainRead = plainRead,
        technicalSetup = technicalSetup,
        setupNetBias = setupNetBias,
        setupConfidence = setupConfidence,
        conditionChips = conditionChips?.map { it.toDomain() },
        regimeAtAnalysis = regimeAtAnalysis,
        previousClose = previousClose,
        previousSetup = previousSetup,
        previousNetBias = previousNetBias,
        setupChanged = setupChanged,
        chipsAdded = chipsAdded,
        chipsRemoved = chipsRemoved,
        headlineMetrics = headlineMetrics?.toDomain(),
        hasDirectNews = hasDirectNews,
        topHeadline = topHeadline,
        contentFlags = contentFlags,
        detailVersion = detailVersion,
        timestamp = timestamp
    )
}

fun NetworkConditionChip.toDomain(): DomainConditionChip {
    return DomainConditionChip(
        label = label,
        direction = direction,
        category = category,
        qualifiedBy = qualifiedBy
    )
}

fun NetworkHeadlineMetrics.toDomain(): DomainHeadlineMetrics {
    return DomainHeadlineMetrics(
        distSma200Pct = distSma200Pct,
        rvol = rvol,
        atrPct = atrPct,
        rangePosition52w = rangePosition52w,
        rsi = rsi
    )
}

// ==========================================
// 🌐 NETWORK TO DOMAIN — DETAIL
// ==========================================

/**
 * Unlike the preview list, `symbol` is passed in explicitly rather than trusted from the JSON
 * body — the caller (`RemoteStockDataSourceImpl`) already knows which symbol it requested via
 * `GET /stocks/{symbol}/detail`, so the mapper can't silently disagree with the actual fetch
 * context (same reasoning the old `NetworkStockDeepStudy.toDomain()` used).
 */
fun NetworkStockDetail.toDomain(symbol: String, lastSyncedTimestamp: Long): StockDetail {
    return StockDetail(
        symbol = symbol,
        lastSyncedTimestamp = lastSyncedTimestamp,
        detailVersion = detailVersion,
        analysisDate = analysisDate,
        technicalIndicators = technicalIndicators?.toDomain(),
        levels = levels?.toDomain(),
        setupSignals = setupSignals?.map { it.toDomain() },
        setupConfirming = setupConfirming,
        setupConflicting = setupConflicting,
        conditionLabels = conditionLabels?.map { it.toDomain() },
        watchList = watchList?.map { it.toDomain() },
        fundamentals = fundamentals?.toDomain(),
        macro = macro?.toDomain(),
        technicalRead = technicalRead,
        notCovered = notCovered,
        scenarios = scenarios?.map { it.toDomain() },
        considerations = considerations,
        executiveThesis = executiveThesis?.toDomain(),
        topNewsStream = topNewsStream?.map { it.toDomain() },
        contextVault = contextVault?.toDomain(),
        calls = calls?.toDomain(),
        timestamp = timestamp
    )
}

fun NetworkTechnicalIndicators.toDomain(): DomainTechnicalIndicators {
    return DomainTechnicalIndicators(
        rsi = rsi,
        sma20 = sma20,
        sma50 = sma50,
        sma200 = sma200,
        macdSignal = macdSignal,
        macdHistogram = macdHistogram,
        barsSinceMacdCross = barsSinceMacdCross,
        latestVolume = latestVolume,
        avgVolume20d = avgVolume20d,
        atrPct = atrPct,
        realizedVol = realizedVol,
        volPercentile = volPercentile,
        rvol = rvol,
        upDownVolumeRatio = upDownVolumeRatio,
        distSma20Pct = distSma20Pct,
        distSma50Pct = distSma50Pct,
        distSma200Pct = distSma200Pct,
        high52w = high52w,
        low52w = low52w,
        rangePosition52w = rangePosition52w,
        returns = returns?.toDomain()
    )
}

fun NetworkReturns.toDomain(): DomainReturns {
    return DomainReturns(d1 = d1, w1 = w1, m1 = m1, m3 = m3, m6 = m6, y1 = y1)
}

fun NetworkLevels.toDomain(): DomainLevels {
    return DomainLevels(
        support = support,
        resistance = resistance,
        accumulationZoneLow = accumulationZoneLow,
        distributionZoneHigh = distributionZoneHigh,
        fairValueAnchor = fairValueAnchor,
        commentary = commentary
    )
}

fun NetworkSetupSignal.toDomain(): DomainSetupSignal {
    return DomainSetupSignal(label = label, direction = direction)
}

fun NetworkConditionLabel.toDomain(): DomainConditionLabel {
    return DomainConditionLabel(
        label = label,
        meaning = meaning,
        direction = direction,
        category = category,
        qualifiedBy = qualifiedBy
    )
}

fun NetworkWatchItem.toDomain(): DomainWatchItem {
    return DomainWatchItem(kind = kind, text = text, urgency = urgency)
}

fun NetworkFundamentals.toDomain(): DomainFundamentals {
    return DomainFundamentals(
        analystCount = analystCount,
        beta = beta,
        capexToOcfPct = capexToOcfPct,
        consensusStrengthPct = consensusStrengthPct,
        currentRatio = currentRatio,
        debtToEquity = debtToEquity,
        earningsEpsEstimate = earningsEpsEstimate,
        earningsYieldPct = earningsYieldPct,
        epsGrowthYoyPct = epsGrowthYoyPct,
        evEbitda = evEbitda,
        evRevenue = evRevenue,
        fcfYieldPct = fcfYieldPct,
        fetchedAt = fetchedAt,
        forwardEps = forwardEps,
        freeCashFlow = freeCashFlow,
        fundamentalsVersion = fundamentalsVersion,
        grossMarginPct = grossMarginPct,
        lastEarningsDate = lastEarningsDate,
        marketCap = marketCap,
        nextEarningsDate = nextEarningsDate,
        nextEarningsIsEstimate = nextEarningsIsEstimate,
        operatingCashFlow = operatingCashFlow,
        operatingMarginPct = operatingMarginPct,
        peForward = peForward,
        peHistory5y = peHistory5y,
        peMedian5y = peMedian5y,
        pePercentile5y = pePercentile5y,
        peSpread = peSpread,
        peTrailing = peTrailing,
        peVsMedianPct = peVsMedianPct,
        pegRatio = pegRatio,
        priceToBook = priceToBook,
        priceToSales = priceToSales,
        profitMarginPct = profitMarginPct,
        recommendationKey = recommendationKey,
        recommendationMean = recommendationMean,
        revenueGrowthYoyPct = revenueGrowthYoyPct,
        roaPct = roaPct,
        roePct = roePct,
        roicPct = roicPct,
        sources = sources,
        symbol = symbol,
        targetDispersionPct = targetDispersionPct,
        targetHigh = targetHigh,
        targetLow = targetLow,
        targetMean = targetMean,
        totalCash = totalCash,
        totalDebt = totalDebt,
        trailingEps = trailingEps
    )
}

fun NetworkMacro.toDomain(): DomainMacro {
    return DomainMacro(
        benchmark = benchmark,
        excessEarningsYieldPct = excessEarningsYieldPct,
        excessReturn21dPct = excessReturn21dPct,
        excessReturn63dPct = excessReturn63dPct,
        rateCorrelation63d = rateCorrelation63d,
        rateSensitivity = rateSensitivity,
        riskFree10yPct = riskFree10yPct
    )
}

fun NetworkScenario.toDomain(): DomainScenario {
    return DomainScenario(
        direction = direction,
        whatItWouldMean = whatItWouldMean,
        whatWouldNeedToHappen = whatWouldNeedToHappen
    )
}

fun NetworkExecutiveThesis.toDomain(): DomainExecutiveThesis {
    return DomainExecutiveThesis(
        macroTransmissionVector = macroTransmissionVector,
        whatTheNumbersSay = whatTheNumbersSay,
        valuationContext = valuationContext
    )
}

fun NetworkStockNewsItem.toDomain(): DomainStockNewsItem {
    return DomainStockNewsItem(
        headline = headline,
        sourceDate = sourceDate,
        url = url,
        articleSynthesis = articleSynthesis,
        transmissionMechanism = transmissionMechanism,
        forwardImplication = forwardImplication,
        impactMagnitude = impactMagnitude
    )
}

fun NetworkContextVault.toDomain(): DomainContextVault {
    return DomainContextVault(
        thirtyDayTrendTimeline = thirtyDayTrendTimeline,
        eventLog = eventLog?.map { it.toDomain() }
    )
}

fun NetworkEventLogItem.toDomain(): DomainEventLogItem {
    return DomainEventLogItem(date = date, direction = direction, kind = kind, text = text)
}

fun NetworkCallRecord.toDomain(): DomainCallRecord {
    return DomainCallRecord(
        open = open?.map { it.toDomain() },
        resolved = resolved?.map { it.toDomain() },
        stats = stats?.toDomain()
    )
}

fun NetworkForwardCall.toDomain(): DomainForwardCall {
    return DomainForwardCall(
        id = id,
        madeOn = madeOn,
        resolveDate = resolveDate,
        predicate = predicate,
        level = level,
        levelUpper = levelUpper,
        priceWhenMade = priceWhenMade,
        statement = statement,
        basis = basis,
        status = status,
        resolvedOn = resolvedOn,
        resolvedPrice = resolvedPrice,
        outcomeNote = outcomeNote
    )
}

fun NetworkCallStats.toDomain(): DomainCallStats {
    return DomainCallStats(
        totalResolved = totalResolved,
        held = held,
        failed = failed,
        hitRatePct = hitRatePct,
        byBasis = byBasis?.mapValues { it.value.toDomain() }
    )
}

fun NetworkCallBasisStat.toDomain(): DomainCallBasisStat {
    return DomainCallBasisStat(n = n, held = held)
}

// ==========================================
// 💾 DOMAIN TO ENTITY (Room Database Write)
// ==========================================

/** Near 1:1 field copy — nested Domain* types are reused directly as Entity columns; `StocksConverters` handles their JSON (de)serialization. */
fun StockPreview.toEntity(): StockPreviewEntity {
    return StockPreviewEntity(
        symbol = symbol,
        lastSyncedTimestamp = lastSyncedTimestamp,
        name = name,
        schemaVersion = schemaVersion,
        analysisDate = analysisDate,
        price = price,
        changePercent = changePercent,
        plainRead = plainRead,
        technicalSetup = technicalSetup,
        setupNetBias = setupNetBias,
        setupConfidence = setupConfidence,
        conditionChips = conditionChips,
        regimeAtAnalysis = regimeAtAnalysis,
        previousClose = previousClose,
        previousSetup = previousSetup,
        previousNetBias = previousNetBias,
        setupChanged = setupChanged,
        chipsAdded = chipsAdded,
        chipsRemoved = chipsRemoved,
        headlineMetrics = headlineMetrics,
        hasDirectNews = hasDirectNews,
        topHeadline = topHeadline,
        contentFlags = contentFlags,
        detailVersion = detailVersion,
        timestamp = timestamp
    )
}

fun StockDetail.toEntity(): StockDetailEntity {
    return StockDetailEntity(
        symbol = symbol,
        lastSyncedTimestamp = lastSyncedTimestamp,
        detailVersion = detailVersion,
        analysisDate = analysisDate,
        technicalIndicators = technicalIndicators,
        levels = levels,
        setupSignals = setupSignals,
        setupConfirming = setupConfirming,
        setupConflicting = setupConflicting,
        conditionLabels = conditionLabels,
        watchList = watchList,
        fundamentals = fundamentals,
        macro = macro,
        technicalRead = technicalRead,
        notCovered = notCovered,
        scenarios = scenarios,
        considerations = considerations,
        executiveThesis = executiveThesis,
        topNewsStream = topNewsStream,
        contextVault = contextVault,
        calls = calls,
        timestamp = timestamp
    )
}

// ==========================================
// 💾 ENTITY TO DOMAIN (Room Database Read)
// ==========================================

fun StockPreviewEntity.toDomain(): StockPreview {
    return StockPreview(
        symbol = symbol,
        lastSyncedTimestamp = lastSyncedTimestamp,
        name = name,
        schemaVersion = schemaVersion,
        analysisDate = analysisDate,
        price = price,
        changePercent = changePercent,
        plainRead = plainRead,
        technicalSetup = technicalSetup,
        setupNetBias = setupNetBias,
        setupConfidence = setupConfidence,
        conditionChips = conditionChips,
        regimeAtAnalysis = regimeAtAnalysis,
        previousClose = previousClose,
        previousSetup = previousSetup,
        previousNetBias = previousNetBias,
        setupChanged = setupChanged,
        chipsAdded = chipsAdded,
        chipsRemoved = chipsRemoved,
        headlineMetrics = headlineMetrics,
        hasDirectNews = hasDirectNews,
        topHeadline = topHeadline,
        contentFlags = contentFlags,
        detailVersion = detailVersion,
        timestamp = timestamp
    )
}

fun StockDetailEntity.toDomain(): StockDetail {
    return StockDetail(
        symbol = symbol,
        lastSyncedTimestamp = lastSyncedTimestamp,
        detailVersion = detailVersion,
        analysisDate = analysisDate,
        technicalIndicators = technicalIndicators,
        levels = levels,
        setupSignals = setupSignals,
        setupConfirming = setupConfirming,
        setupConflicting = setupConflicting,
        conditionLabels = conditionLabels,
        watchList = watchList,
        fundamentals = fundamentals,
        macro = macro,
        technicalRead = technicalRead,
        notCovered = notCovered,
        scenarios = scenarios,
        considerations = considerations,
        executiveThesis = executiveThesis,
        topNewsStream = topNewsStream,
        contextVault = contextVault,
        calls = calls,
        timestamp = timestamp
    )
}
