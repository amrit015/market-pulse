package com.marketlabs.pulse.data.theme

// Generated by Claude Code.

import android.content.Context
import androidx.datastore.preferences.core.Preferences
import androidx.datastore.preferences.core.stringPreferencesKey
import androidx.datastore.preferences.preferencesDataStore

/**
 * DataStore key definitions for the persisted theme preset. Single key, storing
 * `MarketPulseTheme.name` as a plain string — `ThemeRepository` owns parsing it back to the enum.
 */
object ThemePreferences {
    val SELECTED_THEME: Preferences.Key<String> = stringPreferencesKey("selected_theme")
}

val Context.themeDataStore by preferencesDataStore(name = "theme_preferences")
