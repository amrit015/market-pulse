package com.marketlabs.pulse.data.legal

// Generated by Claude Code.

import android.content.Context
import androidx.datastore.preferences.core.Preferences
import androidx.datastore.preferences.core.intPreferencesKey
import androidx.datastore.preferences.core.longPreferencesKey
import androidx.datastore.preferences.preferencesDataStore

/**
 * DataStore key definitions for the local-only legal-acceptance record — same shape as
 * `ThemePreferences`. No device ID / auth tracking: just the accepted legal version and when.
 */
object LegalPreferences {
    val ACCEPTED_VERSION: Preferences.Key<Int> = intPreferencesKey("accepted_version")
    val ACCEPTED_AT_EPOCH_MILLIS: Preferences.Key<Long> = longPreferencesKey("accepted_at_epoch_millis")
}

val Context.legalDataStore by preferencesDataStore(name = "legal_preferences")
