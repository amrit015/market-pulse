package com.marketlabs.pulse.ui.screens.stocks

// Generated by Claude Code.

import com.marketlabs.pulse.storage.model.stocks.StockDetail

/**
 * Drives the pushed stock-detail screen (navigated to by tapping a preview on the Analysis tab).
 * `symbol` is always known immediately (it comes from the nav argument, not a network fetch) so
 * it's non-null from the first emission — the future Screen can render a title/header ("AAPL")
 * before `detail` itself has loaded. `detail` stays null until the first successful fetch or cache
 * hit; there's no sealed Loading/Success/Error split here because "detail is null" plus
 * `isLoading`/`errorMessage` is a flag difference, not a genuinely different layout, per this
 * project's UiState convention.
 */
data class StockDetailUiState(
    val symbol: String,
    val detail: StockDetail? = null,
    val isLoading: Boolean = false,
    val isRefreshing: Boolean = false,
    val errorMessage: String? = null
)
