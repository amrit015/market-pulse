package com.marketlabs.pulse.ui.settings

// Generated by Claude Code.

import android.widget.Toast
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.ui.platform.LocalContext
import androidx.hilt.navigation.compose.hiltViewModel
import androidx.lifecycle.compose.collectAsStateWithLifecycle
import androidx.compose.ui.res.stringResource
import com.marketlabs.pulse.R

/**
 * Stateful — `hiltViewModel()`, `collectAsStateWithLifecycle()`, per this repo convention. Owns the
 * `Toast` side effect for the three "More" placeholder items (Notifications / Data & Sync / About)
 * so `SettingsScreen` itself stays a pure function of its parameters — per the spec, those three are
 * stubs only ("tapping them navigates to a stub ComingSoonScreen or a Toast. Do not implement these
 * sub-screens"); a `Toast` was chosen over a stub screen/route to avoid adding navigation surface
 * for something explicitly out of scope.
 */
@Composable
fun SettingsRoute(
    onNavigateUp: () -> Unit,
    viewModel: SettingsViewModel = hiltViewModel()
) {
    val uiState by viewModel.uiState.collectAsStateWithLifecycle()
    val context = LocalContext.current
    val comingSoonMessage = stringResource(id = R.string.settings_coming_soon_toast)

    SettingsScreen(
        uiState = uiState,
        onThemeSelected = viewModel::selectTheme,
        onNavigateUp = onNavigateUp,
        onMoreItemClick = {
            Toast.makeText(context, comingSoonMessage, Toast.LENGTH_SHORT).show()
        }
    )
}
