package com.marketlabs.pulse.ui.screens.tutorials

// Generated by Claude Code.

import androidx.compose.runtime.Composable
import androidx.compose.ui.res.stringResource
import androidx.compose.ui.tooling.preview.Preview
import com.marketlabs.pulse.R
import com.marketlabs.pulse.ui.components.DocumentSectionsScreen
import com.marketlabs.pulse.ui.components.tutorials.GaugeAnatomyDiagram
import com.marketlabs.pulse.ui.theme.MarketPulseTheme

/**
 * spec-20260917-content-refinement.md Pass 3a -- new standalone Tutorials article, added because
 * Pass 2 exposed a real inconsistency: most gauges read their raw *level* against fixed bands, but
 * a few read direction-of-change instead, and two are polarity-inverted. This teaches the one
 * shared pattern (raw value -> band -> color) and names the exceptions, with D4 (Pass 4) as its
 * visual spine right after the 3-step explanation.
 *
 * Also carries Pass 3b's two naming-collision glossary notes ("positioning," "market regime") as a
 * closing section -- this app has no live tap target that naturally reaches a definition-only entry
 * with no bands/no metric id, so rather than force new navigation plumbing for two low-priority
 * notes, they're placed here: a primer about how to read this app's own terminology is the most
 * natural home for "two words this app uses twice."
 */
@Composable
fun TutorialsGaugeAnatomyScreen(onNavigateUp: () -> Unit) {
    val sections = listOf(
        stringResource(id = R.string.tutorials_gauge_anatomy_steps_title) to
            stringResource(id = R.string.tutorials_gauge_anatomy_steps_body),
        stringResource(id = R.string.tutorials_gauge_anatomy_exceptions_title) to
            stringResource(id = R.string.tutorials_gauge_anatomy_exceptions_body),
        stringResource(id = R.string.tutorials_gauge_anatomy_naming_title) to
            stringResource(id = R.string.tutorials_gauge_anatomy_naming_body)
    )

    DocumentSectionsScreen(
        title = stringResource(id = R.string.tutorials_item_gauge_anatomy),
        sections = sections,
        onNavigateUp = onNavigateUp,
        showFooter = true,
        diagrams = mapOf(0 to { GaugeAnatomyDiagram() })
    )
}

// ============================================================================
// 🎨 PREVIEWS
// ============================================================================

@Preview(name = "Light", showBackground = true)
@Composable
private fun PreviewTutorialsGaugeAnatomyScreenLight() {
    MarketPulseTheme(theme = MarketPulseTheme.NAVY) {
        TutorialsGaugeAnatomyScreen(onNavigateUp = {})
    }
}

@Preview(name = "Dark", showBackground = true)
@Composable
private fun PreviewTutorialsGaugeAnatomyScreenDark() {
    MarketPulseTheme(theme = MarketPulseTheme.LILAC) {
        TutorialsGaugeAnatomyScreen(onNavigateUp = {})
    }
}
